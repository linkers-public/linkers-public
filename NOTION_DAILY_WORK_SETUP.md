# Notion 일일 작업 일지 자동 업데이트 설정 가이드

GitHub Actions를 통해 매일 자동으로 오늘의 작업 내용을 Notion에 업데이트하는 시스템입니다.

## 📋 기능

- **자동 실행**: 매일 한국 시간 오전 9시에 자동 실행
- **수동 실행**: GitHub Actions에서 수동으로 실행 가능
- **Push 시 실행**: main 브랜치에 push될 때마다 자동 실행
- **날짜별 페이지**: 날짜별로 작업 일지 페이지 자동 생성/업데이트
- **커밋 정보 수집**: 오늘 커밋된 내용과 변경된 파일 목록 자동 수집

## 🔧 설정 방법

### 1. Notion Integration 생성

1. [Notion Integrations](https://www.notion.so/my-integrations) 페이지로 이동
2. "New integration" 클릭
3. Integration 이름 입력 (예: "GitHub Actions Work Log")
4. Workspace 선택
5. "Submit" 클릭
6. **Internal Integration Token** 복사 (나중에 사용)

### 2. Notion 부모 페이지 준비

1. Notion에서 작업 일지를 저장할 부모 페이지 생성 (예: "📅 일일 작업 일지")
2. 생성한 Integration을 해당 페이지에 연결:
   - 페이지 우측 상단 "..." 메뉴 클릭
   - "Connections" 선택
   - 방금 생성한 Integration 선택

### 3. 페이지 ID 확인

부모 페이지의 URL에서 페이지 ID를 추출합니다:
```
https://www.notion.so/Your-Page-Title-abc123def456ghi789jkl012
                                    ↑ 이 부분이 페이지 ID
```

페이지 ID는 32자리 16진수 문자열입니다. 하이픈을 제거한 형태로 사용합니다:
- 예: `abc123def456ghi789jkl012mno345pqr678`

### 4. GitHub Secrets 설정

GitHub 저장소의 Settings → Secrets and variables → Actions에서 다음 Secrets를 추가합니다:

#### `NOTION_TOKEN`
- 값: Notion Integration Token (1단계에서 복사한 값)
- 예: `secret_abc123def456...`

#### `NOTION_PARENT_PAGE_ID`
- 값: 부모 페이지 ID (3단계에서 확인한 값, 하이픈 제거)
- 예: `abc123def456ghi789jkl012mno345pqr678`

## 📝 사용 방법

### 자동 실행

워크플로우는 다음 경우에 자동으로 실행됩니다:

1. **매일 자정 (UTC) = 한국 시간 오전 9시**
   - `.github/workflows/update-notion-daily.yml`의 `schedule` 설정에 따라 실행

2. **main 브랜치에 push될 때**
   - 코드를 push하면 자동으로 오늘의 작업 내용이 업데이트됩니다

### 수동 실행

1. GitHub 저장소의 "Actions" 탭으로 이동
2. "Update Notion Daily Work Log" 워크플로우 선택
3. "Run workflow" 버튼 클릭
4. 브랜치 선택 후 "Run workflow" 클릭

## 📊 생성되는 페이지 구조

각 날짜별로 다음과 같은 구조의 페이지가 생성됩니다:

```
2025-01-15 작업 일지
├── 📋 오늘의 작업 내용
│   ├── 커밋 1: 기능 추가
│   │   ├── 커밋 해시: abc123
│   │   ├── 작성자: 홍길동
│   │   ├── 시간: 2025-01-15 14:30:00
│   │   ├── 변경된 파일: 5개
│   │   └── 변경된 파일:
│   │       - src/components/Button.tsx
│   │       - src/utils/helpers.ts
│   │       ...
│   ├── 커밋 2: 버그 수정
│   │   ...
│   └── ...
└── 📊 통계
    ├── 총 커밋 수: 3개
    └── 총 변경 파일 수: 12개
```

## 🔍 로컬에서 테스트

로컬에서 스크립트를 테스트하려면:

```bash
# 환경 변수 설정
export NOTION_TOKEN="your_notion_token"
export NOTION_PARENT_PAGE_ID="your_parent_page_id"

# 스크립트 실행
npm run update-notion
```

## ⚠️ 주의사항

1. **시간대**: 스크립트는 한국 시간(KST, UTC+9) 기준으로 오늘 날짜를 계산합니다.
2. **Git 히스토리**: 워크플로우는 전체 Git 히스토리를 가져오므로(`fetch-depth: 0`) 실행 시간이 다소 걸릴 수 있습니다.
3. **Notion API 제한**: Notion API는 분당 요청 수에 제한이 있습니다. 너무 많은 커밋이 있으면 일부가 누락될 수 있습니다.
4. **페이지 권한**: Integration이 부모 페이지에 접근 권한이 있어야 합니다.

## 🐛 문제 해결

### "NOTION_TOKEN이 설정되지 않았습니다" 오류
- GitHub Secrets에 `NOTION_TOKEN`이 올바르게 설정되었는지 확인하세요.

### "NOTION_PARENT_PAGE_ID가 설정되지 않았습니다" 오류
- GitHub Secrets에 `NOTION_PARENT_PAGE_ID`가 올바르게 설정되었는지 확인하세요.

### "페이지를 찾을 수 없습니다" 오류
- Integration이 부모 페이지에 연결되어 있는지 확인하세요.
- 페이지 ID가 올바른지 확인하세요 (하이픈 제거).

### "권한이 없습니다" 오류
- Notion Integration이 부모 페이지에 접근 권한이 있는지 확인하세요.
- 페이지의 "Connections"에서 Integration이 연결되어 있는지 확인하세요.

## 📚 참고 자료

- [Notion API 문서](https://developers.notion.com/)
- [GitHub Actions 문서](https://docs.github.com/en/actions)
- [Notion Integration 가이드](https://developers.notion.com/docs/getting-started)

